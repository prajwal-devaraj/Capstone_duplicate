# SmartSpend Frontend (Vite + React)

## Run
```bash
cd frontend
npm install
npm run dev
```

It will try to reach the backend at `http://127.0.0.1:5000`. Set `VITE_API_BASE_URL` in `frontend/.env` if needed.
