# SmartSpend MongoDB Schema (reference)
Collections: users, income, transactions, expenses, bills, insights, goals, notifications, achievements, forecast_groundtruth, flag_patterns, dashboard, current_balance, perday, month, accumulate.
See README for key fields and indexes.
